<div class="row justify-content-end" >
    <%= form_with url: search_path, method: :get, local: true do |f| %>
     <table　float="right">
        <tr>
          <td><%= f.text_field 'search[value]', size: "20x2" %></td>
          <!--<td>%= f.select 'search[how]', options_for_select({ "部分一致" => "partical", "前方一致" => "forward", "後方一致" => "backward", "完全一致" => "match" }) %></td>-->
          <td><%= f.submit :"Search" %></td>
       </tr>
     </table>
    <% end %>
</div>